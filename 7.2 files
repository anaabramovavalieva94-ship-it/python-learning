# Use the file name mbox-short.txt as the file name
fname = input("Enter file name: ")
fh = open(fname)
count=0
total=0.0
for line in fh:
    if not line.startswith("X-DSPAM-Confidence:"):
        continue
       
    cal_pol=line.find(':')
    value=float(line[cal_pol+1:].strip())
    
    count=count+1
    total=total+value

average=total/count
print('Average spam confidence:', average)
